% !TEX root = ../report.tex

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc}
\usepackage{float}

\usepackage{xcolor}
%\usepackage{colortbl}

\usepackage{fontspec}
\setmainfont{Times New Roman}
\setmonofont{Courier New}
\usepackage[nopatch=footnote]{microtype}
\usepackage{pgffor}  % для циклів \foreach
\usepackage{enumitem}
\usepackage{bm}
\usepackage{tabularray}

\sloppy

\usepackage{lastpage} % Для підрахунку сторінок
\usepackage[figure,table]{totalcount}
\usepackage{etoolbox}

\newcounter{totrefs}
\AtEveryBibitem{\stepcounter{totrefs}} % Додає +1 за кожне джерело у списку
\makeatletter
\AtEndDocument{%
  % Примусово записує фінальне число у допоміжний файл
  \immediate\write\@mainaux{\string\gdef\string\totbib{\number\value{totrefs}}}%
}
\makeatother
\providecommand{\totbib}{???} % Заглушка на випадок першої компіляції

%\usepackage[normalem]{ulem} % для подчёркиваний uline
%\ULdepth = 0.16em % расстояние от линии до текста выше/ниже